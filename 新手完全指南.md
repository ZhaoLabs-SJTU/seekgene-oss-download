# 🌟 新手完全指南 — seekgene-oss-download

> 从零开始，把测序服务商「数据释放」邮件里的数据，用脚本一键下载到本地/服务器，并完成完整性校验。

## 适用人群
- 第一次收到测序服务商「数据释放」邮件、不知道如何批量下载的组学小白
- 数据量很大（上百 GB）、不想逐个手动点下载的用户

## 你需要准备
- 一封「数据释放」邮件（`.eml` 文件，里面含下载链接和 AccessKey）
- Linux / macOS（Windows 用 WSL）
- Python 3.8+
- 一块足够大的下载磁盘

## 第一章 安装依赖（约 2 分钟）

```bash
python3 -m pip install oss2
```

## 第二章 下载仓库并配置凭据（约 5 分钟）

```bash
git clone https://github.com/ZhaoLabs-SJTU/seekgene-oss-download.git
cd seekgene-oss-download

# 把邮件里的信息填到环境变量（凭据不要写进代码/命令行历史）
export OSS_ACCESS_KEY_ID="你的 AccessKeyId"
export OSS_ACCESS_KEY_SECRET="你的 AccessKeySecret"
export OSS_ENDPOINT="oss-cn-beijing.aliyuncs.com"
export OSS_BUCKET="邮件里的 Bucket"
export OSS_PREFIX="release/temp-.../"     # 邮件里的目录前缀
export LOCAL_ROOT="/path/to/download/dir" # 你想下载到哪
```

## 第三章 解析邮件（约 1 分钟）

```bash
python3 scripts/parse_email.py "数据释放邮件.eml"
```

会打印出 JSON：`access_key_id / access_key_secret / endpoint / bucket / prefix / expire`。
如果脚本没提取出来，也可以手动从邮件正文里抄到上面的环境变量里。

## 第四章 下载数据（约 1 小时，看数据量）

```bash
# ① 先列清单，看看一共要下多少
python3 scripts/list_oss.py > list_objects.txt

# ② 后台批量下载（断点续传，断了重跑即可）
nohup python3 -u scripts/download.py > download.log 2>&1 &

# ③ 随时看进度
python3 scripts/progress.py
```

看到 `ALL_FINISHED` 就是下载完了。

## 第五章 完整性校验（约 10 分钟，必做）

```bash
python3 scripts/verify.py
# 全部通过会输出 ALL_MD5_VERIFIED_OK
```

这一步会把下载的每个文件和邮件里给的 `md5.txt` 逐一比对，确认数据「一个不少、一个没坏」。

## 常见问题

| 现象 | 解决 |
|------|------|
| 下载报 `403` / 链接打不开 | 释放链接有有效期，过期了联系服务商重新释放 |
| 下载到一半断了 | 脚本支持断点续传，直接重新运行第 ② 步即可 |
| 大小看着一样但怕数据坏 | 大小一致 ≠ 完整，务必跑第五章的 MD5 校验 |
| 担心密码泄露 | 全部走环境变量，绝不写进代码、不进 Git 历史 |

## 验收清单
- [ ] 文件数与邮件里描述一致
- [ ] 下载日志出现 `ALL_FINISHED`
- [ ] `verify.py` 输出 `ALL_MD5_VERIFIED_OK`
- [ ] 无 `.part` 残留文件

> 📖 完整流程、故障排查与速查表见 [SKILL.md](SKILL.md)；Word/Excel/PPT 三件套见 [小白文档](小白文档/)。
